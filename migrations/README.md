# Migrations

Database migrations will be introduced with the message store. Migrations must
be reviewed against both SQLite and PostgreSQL until separate dialect-specific
paths are deliberately adopted.
